# scripts/

Documented in **[docs/available_scripts.md](../docs/available_scripts.md)** — what each
script owns, what it reads and writes, and how it avoids overwriting hand-drawn art.

Kept as a pointer rather than a copy so the two can't drift.

```bash
pip install -r requirements.txt
```
