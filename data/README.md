# data/

**Dormant.** Music is out of scope and the vision is being reworked — see
`docs/DATA_MODEL.md`. Nothing in the game reads these files right now.

- `library.sample.json` — placeholder shape from the music direction (an object with
  `meta`, `eras[]`, `songs[]`). **Aditya owns whatever replaces it, if anything does.**
- `library.json` — never committed (gitignored). It was the enriched working copy,
  regenerated by `scripts/enrich_songs.py`. That script was deleted along with the rest of
  `scripts/`; restore it with `git checkout cff5a68 -- scripts/` if music comes back.
